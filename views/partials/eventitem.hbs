
  <img src="{{photo}}" alt="photo">
  <div class="column-container">
    <div class="event-name-description">
     <h5>Event : {{title}}</h5>
     <div class="description-text">
      <p>{{description}}</p>
    </div>
     <!--p>Start : {{date}}</p-->
      <div class="dates">
        <p><i class="far fa-calendar-alt"></i>{{{formatDate date}}}</p>
        <!--p>End : {{endDate}}</p-->
        <p><i class="far fa-calendar-alt"></i>{{{formatDate endDate}}}</p>
      </div>
    </div>
    
      <!--p>Created by: {{creator.name}} on {{createdDate}}</p>
      <span>Latitude: {{location.coordinates.[1]}}</span>
      <span>Longitude: {{location.coordinates.[0]}}</span>

      <p>Last updated: {{updatedDate}}</p-->
   
  </div>
